# 要件定義ゲーム 完成度向上計画

最終更新: 2026-09-02

## 1. 目的

このゲームを、機能が多いゲームから「研修の場で迷わず進行でき、議論が自然に深まり、終了後に学びが残るゲーム」へ引き上げる。

改善の判断基準は次の3つとする。

1. **ユーザー体験**: プレイヤーが今やること・次にやることを迷わない
2. **hostの操作性**: hostが複数ルーム、タイマー、質問、イベント、採点を安全に捌ける
3. **シナリオの面白さ**: 質問・対立・制約・事件がつながり、チームごとに違う判断が生まれる

## 2. 現状認識

### 2.1 すでにできていること

- ロビーから結果発表までの6フェーズ、準備OK、タイマー、ルーム対抗戦が実装済み
- hostはルームタブを切り替え、要求カード・質問・仕様書・採点結果をルーム単位で確認できる
- ヒアリング型では、AI自動回答とhost手動回答を切り替えられる。AI失敗時は質問キューへ戻る
- hostのリロード復帰、プレイヤーのトークン復帰、閲覧者ごとのスナップショットフィルタがある
- プレイヤーには他人の秘密情報・NPCの知識・他ルームの内容を渡さないテストがある
- 現時点の品質ベースラインは、ゲームロジック63件・Codex接続確認3件（計66件）のテスト成功、本番ビルド成功
- hostはCodex CLIのローカルブリッジを使い、APIキーやAI回答のコピペなしで採点・NPC回答・イベント・ファシリ補助を実行できる

### 2.2 体験上の主な課題

#### プレイヤー側

- 議論フェーズにはヒアリング・要求カード・仕様書のクイック導線を追加したが、初回参加者が迷わず使えるかは研修環境で未計測
- フェーズごとの目的・必須行動・完了条件・次の行動はガイドにまとめたが、初回参加者が一人で使えるかは研修環境で未計測
- 仕様書は編集者・保存時刻・競合確認を表示するが、同じ欄の同時編集を統合する共同編集機能はまだない
- 接続中・回答待ち・自分の未投票などはガイドと状態別バッジに集約したが、待ち状態の理由が初見で伝わるかは未計測
- 主要入力にはラベルとキーボード操作を追加したが、入力ミス時の文言や支援技術での理解には改善余地がある

#### host側

- 現在のhost画面は、進行操作、ルーム巡回、質問キュー、NPC回答、イベント、AI設定、採点、ロール秘密を一画面に並べている
- ルームタブには人数・準備OK・未回答質問・得点と優先度を表示できるようになったが、10ルーム相当で5秒以内に特定できるかは未計測
- フェーズ移動はステッパーから直接ジャンプでき、影響確認も出るが、hostが確認を迷わず使えるかは未計測
- タイマーはフェーズごとの推奨時間、状態別主ボタン、時間切れの音・表示・延長導線を備えたが、研修中の運用判断は未計測
- AIの実行、手動貼り付け、失敗時の再試行は処理状態付きで確認でき、host PCのCodex CLIへ直接つなぐローカルブリッジも利用できる
- ブラウザ操作を通したE2Eテストは、CIでhost 1画面とプレイヤー2画面の主要フローを検証する形で追加済み

#### シナリオ側

- `restaurant` は、3つの台本イベント、10個の隠れ要件、7つの実運用テストを持ち、質問から事故までの流れが比較的厚い
- `smart-factory`、`kintai`、`supermarket` は、各3イベント・5隠れ要件・5テストケースへ拡充し、イベント案も発生条件・狙い・難易度付きになった
- `beats` の因果チェーンで導入・探索・圧力・決断・結果のつながりを確認できるようにしたが、秘密条件・イベント・テストケース間の参照整合性は自動検証していない
- 因果チェーンはhostと採点AIへ渡るため、同じ事件に対する複数の解決策とトレードオフの説明はシナリオデータ側で継続的に点検する

## 3. プロダクト方針

### 方針A: 常に「次の一手」を見せる

各画面に、現在の目的、推奨アクション、完了条件、未対応件数を表示する。情報量は減らすのではなく、優先度順に並べ、詳細は展開できるようにする。

### 方針B: hostは監督、プレイヤーは意思決定者

hostには全体の異常・詰まり・未処理を集約し、プレイヤーには自分のルームの議論と判断に集中できる情報だけを出す。秘密情報の境界は変更しない。

### 方針C: 事件は「驚き」ではなく「判断の結果」を生む

イベントはランダムな横槍ではなく、公開情報・質問・採用した要求・明記した運用ルールと接続する。結果発表では、どの判断がどの事故を防いだかを説明できるようにする。

### 方針D: AIは加速装置であり、必須ルールにしない

AIがなくても同じゲーム体験が成立する。AIを使う場合も、処理中・失敗・手動引き継ぎを常に明示し、API障害で進行が止まらないようにする。

## 4. 優先順位付き改善項目

### P0-1: プレイヤーのフェーズガイドと行動チェックリスト

実装済み: フェーズごとの目的・必須行動・推奨行動・完了条件・未対応件数・準備OKの効果をガイドとして表示し、議論タブには未対応・新着・自分の提出・未投票の状態別バッジを表示する。

各フェーズの冒頭に、次の内容を固定表示する。

- このフェーズの目的
- チームで必ず行うこと
- できれば行うこと
- 完了条件
- 未完了件数（未回答質問、未投票カード、未記入セクションなど）
- 「準備OK」を押すと何が起きるか

議論フェーズの例:

1. NPCまたは仲間から事実を3つ聞く
2. 正常系だけでなく例外を1つ質問する
3. 要求カードを出す
4. 採用したい内容を仕様書に反映する
5. チーム全員が確認して準備OK

タブを残す場合でも、バッジを「件数」だけでなく「未対応」「新着」「自分の担当」に分ける。特にヒアリング型では、質問・要求カード・仕様書の入口を同時に見せるコンパクトなワークスペースを優先する。

### P0-2: host用「進行コックピット」

実装済み: 進行コックピット、ルーム優先キュー、次フェーズ前チェックを追加し、ステッパーからの直接遷移には遷移先と影響の確認を表示する。同一フェーズのクリックは無操作とし、投票フェーズを離れると多数決を確定する。

既存の機能を廃止せず、上部に次のサマリーを追加する。

- 現在のフェーズと残り時間
- 全体の参加者・接続中人数
- ルームごとの準備OK / 未投票 / 未回答質問 / 未採点
- 今すぐ対応すべき項目の優先キュー
- 次のフェーズへ進む前の確認事項

ルームタブは、単なる切り替えではなく「監視レーン」とする。優先度は次の順で表示する。

1. 接続切れ・未回答質問など、進行を止めるもの
2. 準備OKが揃ったルーム
3. 反応が少なく停滞しているルーム
4. 通常状態のルーム

フェーズジャンプは残しつつ、ジャンプ先と影響（タイマークリア、準備OKリセット、投票確定など）を確認画面で表示する。通常進行は「次のフェーズへ」を主導線にする。

### P0-3: タイマー・AI・復旧操作の一貫性

実装済み: AIの処理状態・失敗時フォールバックに加え、Codex CLIをhost PCから呼ぶローカルブリッジを追加した。ブリッジは一時ディレクトリ・読み取り専用・タイムアウト付きで実行し、127.0.0.1のみで待ち受ける。画面から接続確認と手動モードへの切り替えを提供し、接続確認は5秒でタイムアウトしてヘルス応答も検証する。保存データは後方互換でメタデータを持ち、復帰時にゲームコード・フェーズ・保存時刻を一度表示する。

- フェーズごとに推奨時間プリセットを持たせる
- 「開始」「一時停止」「再開」「リセット」を状態に応じて主ボタン1つへ整理する
- 時間切れ時は、音・表示・host向けの次の選択肢（延長 / 次へ / 継続）を出す
- AIは「待機中 / 実行中 / 適用済み / 失敗 / 手動対応済み」を表示する
- AI失敗時はエラーメッセージ、再試行、プロンプトコピー、手動回答を同じカードに置く
- リロード復帰時は、復帰したゲームコード・フェーズ・保存時刻を一度表示する

### P0-4: 仕様書編集の安全性

実装済み: セクションごとの編集者・保存時刻・自分の編集中表示、編集開始時の警告、自動保存状態、競合時のサーバー/下書き比較と選択、hostの対象ルーム表示を実装した。主要入力にはアクセシブルなラベルも付与した。

- 各セクションに「担当」「保存済み時刻」「自分が編集中」を表示する
- 他人が編集を開始した欄は、入力前に警告する
- 自動保存の成功 / 失敗を表示する
- 同時編集時の上書きは、可能であれば直前内容との差分確認を挟む
- hostの編集対象ルームをヘッダーにも常時表示する

初期実装では完全な共同編集基盤を導入せず、上書き事故を見つけやすくする表示と確認を優先する。

### P1-1: シナリオを「因果チェーン」で定義する

実装済み: 任意の `beats` フィールドに場面・発生条件・影響・hostの投げかけ・未対応時の結果を定義し、host専用パネルと採点プロンプトへ渡す。既存の `scriptedEvents` は互換性のため残す。

既存の `scriptedEvents` を置き換えず、後方互換のまま任意フィールドを追加する。

```text
公開状況 → 質問で得られる情報 → 役割間の対立 → チームの判断
       → 発生条件を満たした事件 → 運用上の結果 → 振り返り
```

追加候補のデータ項目:

- `beats`: シナリオの場面一覧（導入 / 探索 / 圧力 / 決断 / 結果）
- `trigger`: 時間、採用カード、未定義のまま残った論点などの発生条件
- `impact`: 仕様・運用・予算・納期への影響
- `facilitatorCue`: hostが投げる一言と、深掘り質問
- `counterfactual`: 要件に入っていた場合 / 入っていなかった場合の差

イベントを増やすだけでなく、同じ事件に対して複数の解決策が成立するようにする。正解を一つに固定せず、トレードオフを採点対象にする。

### P1-2: シナリオ間の厚みを揃える

実装済み: 4シナリオに難易度・推奨人数・推奨時間、ステークホルダー型の最低コンテンツ量、構造化されたイベント案を揃え、検証CLIで必須項目と最低件数を確認する。

`restaurant` をヒアリング型の基準シナリオとし、ステークホルダー型3本を次の最低ラインへ引き上げる。

- 役割間の対立軸: 3本以上
- イベント: 3本以上（導入後の圧力、スコープ揺さぶり、運用事故）
- 隠れ要件: 5本以上
- 実運用テスト: 5本以上（正常系1、境界・例外3、スコープまたは移行1）
- `eventIdeas`: hostが選びやすい発生条件・狙い・難易度を持つ

シナリオごとに、開始前の難易度（初級 / 標準 / 上級）と推奨人数・推奨時間も定義する。

### P1-3: 振り返りをゲーム体験に組み込む

実装済み: 結果発表に判断の足跡、質問・採用要求・記入済み仕様書の記録、個人別の質問数・要求カード・採用数・個人目標得点、見落とし要件、実運用事故、問い直しの観点、ルームごとの議論量・採用数・仕様書の空欄比較を表示し、hostがチームの「次回の一手」を保存して参加者へ共有できるようにした。5人を2ルームに分けたE2Eで比較カード2枚、通常フローで判断材料と個人別サマリーの表示も確認している。

結果発表に次を追加する。

- 得点だけでなく、どの質問・要求・判断が得点に寄与したか
- 見落とした要件について「本来どの相手に何を聞けたか」
- 実運用テストで起きた事故と、仕様書のどの空欄が原因だったか
- 各プレイヤーの個人目標と、チームの合意形成への影響
- チームが次回に一つだけ変える行動

ルーム対抗戦は順位だけで終わらせず、異なるルームが同じ事件にどう対応したかを比較できるようにする。

### P1-4: host向けファシリテーション支援

実装済み: シナリオごとの30秒/2分読み上げ台本、停滞時の3段階ヒント、hostが狙いを確認できる因果チェーン、時間不足時の短縮ルートをhost画面へ集約した。

- 開始前の読み上げ台本（30秒版 / 2分版）
- プレイヤーが止まったときのヒントを3段階で提示
- hostがイベントを出す前に「今出す狙い」と「想定される議論」を表示
- 時間不足時の短縮ルート（質問を絞る、投票を省略する、採点だけ実施）
- AIなしで進行するためのNPC回答例と採点用プロンプトの導線

## 5. 実装フェーズ

### Phase 0: 計測可能な土台（1スプリント）

実装済み: ロジック回帰テスト、6フェーズの代表Snapshot fixture、スナップショットの秘密境界テスト、シナリオ検証CLI、PlaywrightによるブラウザスモークテストをCIへ組み込んだ。

- 現行の表示・権限・秘密フィルタを固定する回帰テストを追加
- 各フェーズの代表的なSnapshot fixtureを作る
- ブラウザで「参加 → 開始 → 議論 → 投票 → 結果」のスモークテストを追加
- シナリオJSONの検証スクリプトを追加する
  - ロール数、秘密条件、参照先ID、イベントID、テストケースID
  - ステークホルダー型の最低コンテンツ量
- 計測はまず録画・観察・アンケートを中心とし、個人情報を含む外部分析基盤は導入しない

### Phase 1: P0のhost改善（1〜2スプリント）

実装済み: 進行コックピット、ルーム優先度、フェーズ遷移確認、状態別タイマー、AIフォールバック、Codexローカルブリッジ、復帰通知を実装した。

- 進行コックピットとルーム優先度表示
- フェーズジャンプの確認と次フェーズ前チェック
- タイマー状態整理、時間切れフロー
- AI処理状態と手動フォールバックの統一
- 復帰・接続状態の表示改善

### Phase 2: P0のプレイヤー改善（1〜2スプリント）

実装済み: フェーズガイドに加え、議論画面の冒頭へヒアリング・要求カード・仕様書のクイック導線と未回答/未提出/未記入数を集約した。タブにも未対応・新着・自分の提出・未投票を状態別に表示し、主要入力には支援技術用ラベルを付け、狭い画面では1列に切り替え、キーボード操作可能なボタンで各タブへ移動できる。

- フェーズガイド、行動チェックリスト
- 未対応 / 新着 / 自分の提出 / 未投票バッジ
- 議論フェーズの主要導線を一画面に集約
- 仕様書の保存状態・編集競合表示
- ラベル、キーボード操作、フォーカス、色以外の状態表現を整備

### Phase 3: P1のシナリオ・学習改善（2スプリント）

実装済み: `beats`による因果チェーン、3本のステークホルダー型シナリオの最低コンテンツ量、結果発表の因果説明・ルーム比較、host向け台本・ヒント・短縮ルートを実装した。

- `beats` / `trigger` / `impact` の追加と後方互換読み込み
- 3つのステークホルダー型シナリオを最低ラインまで拡充
- 結果発表の因果説明とチーム比較
- host向け台本・ヒント・短縮ルート

### 実装・レビュー・コミット記録

計画項目は、実装・自動検証・コミットを分けて追跡できるようにしている。下表のコミットは各項目の主な変更点であり、後続の修正・テストコミットも同じ項目に紐づく。

| 項目 | 主な実装コミット | 検証・レビュー根拠 |
| --- | --- | --- |
| P0-1 プレイヤー導線 | `aa4a3b0` `be0b82b` `6a766ac` `245121f` | フェーズガイド、主要導線、状態バッジ、アクセシビリティ、狭い画面をユニット / E2Eで確認 |
| P0-2 hostコックピット | `51c88c4` `914200c` `6cf8950` | 優先度計算、遷移影響、10ルーム相当の巡回をユニット / E2Eで確認 |
| P0-3 タイマー・AI・復旧 | `a673fe9` `501e632` `c365efc` `09a0c0a` `8e249b8` | 状態別タイマー、AIフォールバック、Codex接続、保存復帰、切断復帰をユニット / E2Eで確認 |
| P0-4 仕様書編集の安全性 | `0ea2fb2` `245121f` | 編集者表示、競合確認、自動保存状態、入力ラベルをユニット / E2Eで確認 |
| P1-1 因果チェーン | `513b8f9` | `beats`の検証、host表示、採点プロンプト連携をユニット / シナリオ検証で確認 |
| P1-2 シナリオの厚み | `7598dfc` `7def65b` `5d9c2da` | 4シナリオのstrict検証（`npm run validate:scenarios:strict`）で確認 |
| P1-3 振り返り | `462bb6f` `fafe2c4` `f476457` | 判断材料、個人サマリー、ルーム比較、次回の一手をユニット / E2Eで確認 |
| P1-4 ファシリテーション | `a7fc7f1` | 台本、3段階ヒント、短縮ルートをhost画面とシナリオ検証で確認 |
| Phase 4 磨き込み | `075ebe1` `8aa08cb` `284b8c9` `ca0f3f5` `2a5e175` | 主要E2E、AIなし、復旧、印刷、Markdown保存を確認。実参加者テストのみ未実施 |

最終確認コマンドは `npm test`（66件）、`npm run build`、`npm run validate:scenarios:strict`、`npm run test:e2e`（4件）とし、変更ごとに差分確認（`git diff --check`）後、個別コミットする。

### Phase 4: 磨き込み（1スプリント）

追加済み: Codex利用、通常進行、AI失敗時の手動引き継ぎ、フェーズジャンプ、リロード復帰、研修後メモをまとめたhost向け進行ガイドを追加した。
自動確認済み: AI無効の主要E2Eフローでプレイヤー1人を遅延300ms・低帯域にしても参加できることを確認し、ブリーフィングでロール入口と最初の行動ガイドが見えること、プレイヤー画面を390px幅に切り替えた議論導線と横スクロールなしも検証している。Codexブリッジの接続確認表示と失敗後の手動採点への切り替え、プレイヤーの途中切断時のhost表示、同じルームURLの再訪によるトークン復帰も表示確認している。仕様書はMarkdownファイル保存と、印刷メディアでゲーム周辺UIを隠し成果物だけを表示できることも確認している。2ルームの優先キューからルームを選択でき、表示後5秒以内に対象タブへ切り替えられることも確認している。
実地確認手順: [Phase 4 ユーザビリティテスト手順](PHASE4_USER_TEST_PROTOCOL.md)に、初見60秒・host巡回・異常系・合否の測定方法をまとめている。実参加者による結果はまだ記録していない。

- 研修環境でのユーザビリティテスト
- 画面幅、低速回線、AIなし、途中切断、hostリロードの確認
- 文言、余白、音、アニメーション、印刷・Markdown出力の調整
- READMEとhost向け進行ガイドの更新

## 6. 受け入れ基準

### UX

- 初回プレイヤーが、参加後60秒以内に「自分の役割」と「最初にすること」を説明できる
- 各フェーズで、未対応の重要タスクが1画面で分かる
- モバイル幅または狭いノートPCでも、主要操作が横スクロールなしで実行できる
- AIなしでも、質問・議論・仕様書・投票・結果発表が完走できる

### host

- 10ルーム相当の状態で、対応が必要なルームを5秒以内に特定できる
- 次のフェーズへ進む前に、未準備・未投票・未回答・未採点の有無を確認できる
- AI失敗後、hostが10秒以内に手動対応または再試行へ移れる
- フェーズジャンプ、リセット、新しいルーム作成で、破壊的な影響を確認できる

### シナリオ

- 各シナリオに、正常系・例外・制約・対立・結果の5要素がある
- 事件は少なくとも1つ、プレイヤーの判断によって結果が変わる
- 結果画面で、見落としが「どの質問・判断の不足だったか」と結び付く
- 2〜3人の少人数でも、欠員ロールの引き継ぎだけで議論が成立する

### 品質・安全性

- 既存の秘密情報保護テストを維持し、追加フィールドも閲覧者別に検証する
- ロジックテスト全件成功、本番ビルド成功
- 主要ユーザーフローのブラウザスモークテスト成功
- 保存データのバージョンアップ時に旧データを壊さないか、復帰テストで確認する

## 7. 効果測定

外部テレメトリーは導入せず、まず各回の[host観察シート](HOST_OBSERVATION_SHEET.md)と終了後アンケートで測る。

- プレイヤーが最初の行動を始めるまでの時間
- hostが未対応ルームを見つけるまでの時間
- 「次に何をすればよいか分からなかった」回答率
- 質問数、例外に関する質問数、採用要求数、仕様書の未記入数
- AIなし / AIありでの進行停止回数
- 結果発表後に参加者が説明できる学びの数
- シナリオ別の盛り上がり、難しさ、納得感、再プレイ意向

最初の目標は、hostが途中で画面を探す時間を減らし、プレイヤーの「待ち時間」を減らすこと。スコア自体を上げることは副目標とし、質問や判断の質が上がった結果として評価する。

## 8. リスクと対策

| リスク | 対策 |
|---|---|
| host画面に情報を足しすぎて、かえって見づらくなる | 重要度順のサマリーを先に出し、詳細は展開式にする。ユーザーテストで確認する |
| イベントを増やしすぎて、hostが処理しきれない | 必須イベントと任意イベントを分け、短縮ルートを用意する |
| AIの回答がシナリオの秘密を先に漏らす | 既存のNPC知識フィルタ、回答プロンプト、回答後のhost確認を維持する |
| 共同編集改善で通信量・競合が複雑になる | まず表示・警告・保存状態を改善し、同期方式の刷新は後段にする |
| シナリオJSONの自由度が上がり、壊れたシナリオが増える | JSON検証とサンプルfixtureを先に追加する |

## 9. 今すぐ着手する最初のタスク

1. 完了: P0の画面仕様を [host用](WIREFRAME_HOST.md) と [プレイヤー用](WIREFRAME_PLAYER.md) の2枚のワイヤーフレームにした
2. 完了: [フェーズガイド](../web/src/game/phaseGuide.ts)で「目的 / 必須行動 / 完了条件 / 表示する未対応件数」を定義した
3. 完了: [ルーム優先度](../web/src/game/engine.ts)を純粋関数として実装し、ロジックテストで検証した
4. 完了: 既存4シナリオのコンテンツ量と因果チェーンを点検した（`npm run validate:scenarios:strict`）
5. 完了: [シナリオJSON検証](../web/scripts/validate-scenarios.mjs)のルールを追加した
6. 完了: [host・プレイヤーのブラウザスモークテスト](../web/e2e/game-flow.spec.ts)を2人構成で通した

### 今回の計画でやらないこと

- 初手からサーバー構成やWebRTC方式を変更すること
- AIを必須化すること
- スコア計算を、画面改善の検証前に大きく変更すること
- 外部分析サービスへ参加者データを送ること

まずは既存のゲームループを壊さず、見落とし・待ち・hostの判断負荷を減らす。そこから、シナリオの因果関係と振り返りを強化する。
